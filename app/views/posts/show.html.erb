<h1><%= @post.title %></h1>

<p><%= @post.body %></p> 

<h3><%= link_to "All Posts", posts_path %> | <%= link_to "Edit Post", edit_post_path(@post) %>
    <%= link_to "Delete post", post_path(@post), data: { 
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"
    }
    %>
</h3>    
